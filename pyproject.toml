[project]
name = "manipulation-game"
version = "0.1.0"
description = "OpenRouter OpenAI-compatible sample"
requires-python = ">=3.9"
dependencies = [
  "openai>=1.40.0",
  "python-dotenv>=1.0.1",
  "sympy>=1.13.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.4.0",
]
